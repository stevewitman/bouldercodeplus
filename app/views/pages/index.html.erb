<h1>Pages</h1>
<div>
  <% if current_user %>
    <%= current_user.name %> | <%= link_to "Sign Out", signout_path %>
  <% else %>
    <%= link_to "Sign in with Meetup.com", "/auth/meetup" %>
  <% end %>
</div>
